[project]
name = "tempus-sdk"
version = "0.1.0"
description = "Official Python SDK for Tempus Pricing Engine"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "httpx>=0.27.0",
    "pydantic>=2.7.0"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
