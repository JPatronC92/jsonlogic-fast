[project]
name = "jsonlogic-fast-dev"
version = "0.1.0"
description = "Development tools for jsonlogic-fast"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "maturin>=1.12.4",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]
